services:
  agent-runner:
    build: .
    volumes:
      # Mount host Docker socket so vllmd can manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Mount the model directory into the tool container
      - ${MODEL_PATH:-/tmp}:/model:ro
    environment:
      - MODEL_PATH=/model
